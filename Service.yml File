Accessing a Service via NodePort

```sh
apiVersion: v1
kind: Service
metadata:
  name: np
spec:
  selector:
     app: nginx
  ports:
     - protocol: TCP
       port: 80
       targetPort: 80
       nodePort: 31111
  type: NodePort
  
  ```
