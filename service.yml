apiVersion: v1
kind: Service
metadata:
  name: np-service
spec:
  type: NodePort
  ports: 
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
      
      
##  nodePort's range (30000-32767)
